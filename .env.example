# ---------------------------------------------------------------------------
# Simola HOA Portal — environment variables
# This is a static informational PWA. There is NO database and NO auth, so the
# only variable is the public site URL (used for SEO metadata, sitemap, robots).
# ---------------------------------------------------------------------------

NEXT_PUBLIC_SITE_URL="http://localhost:3000"
